import React, { Fragment } from "react";
import Track from "../components/Track"; // Fix the file name to be in lowercase

const Tracker = () => (
    <Fragment>
        <Track/>
    </Fragment>
);

export default Tracker;